# Local contributors can run the public board without credentials.
USE_SAMPLE_DATA=true

# Standard PostgreSQL URL. Supabase, Neon, and local Docker all work.
# Vercel should use Supabase's transaction pooler URL; the crawler can use its
# session pooler URL. Keep this server-side and never prefix it with NEXT_PUBLIC_.
DATABASE_URL=postgresql://daily_jobs:daily_jobs@localhost:5432/daily_berlin_jobs
DATABASE_SSL=false
STORAGE_BACKEND=postgres

# Next.js admin -> GitHub Actions dispatch (server-only)
GITHUB_OWNER=your_github_username
GITHUB_REPO=daily-berlin-jobs
GITHUB_WORKFLOW_ID=daily-update.yml
GITHUB_ACTIONS_TOKEN=github_pat_replace_me

# Next.js admin authentication (server-only)
ADMIN_PASSWORD_HASH=replace_with_a_password_hash
SESSION_SECRET=replace_with_a_long_random_secret
